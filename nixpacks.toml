[variables]
NODE_ENV = "production"
NIXPACKS_NODE_VERSION = "20"

[phases.install]
cmds = ["npm ci --include=optional"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start -- -H 0.0.0.0 -p ${PORT:-3000}"
